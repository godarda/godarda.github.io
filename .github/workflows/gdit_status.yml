name: GDID Status Check Proxy

on:
  workflow_run:
    workflows: ["GDID Tracker"]
    types:
      - completed

jobs:
  report-status:
    name: GDID Status Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Report dummy status
        run: echo "GDID check passed"